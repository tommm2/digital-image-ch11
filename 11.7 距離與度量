距離轉換法是計算這種距離的有效方式。按照步驟描述如下：
步驟1：為影像中的每個像素(x,y)都加上一個標記d(x,y)，代表其與 R 的距離。  (R內標記為0,R外標記為無限大)

步驟2：現在逐個像素來檢視影像。 以min{d(x,y) , d(x+1,y)+1 , d(x-1,y)+1 , d(x,y-1)+1 , d(x,y+1)+1 }取代原有之d(x,y)

步驟3：重複步驟2，直到所有標記都已轉換為有限數值。
